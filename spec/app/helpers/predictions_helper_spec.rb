require 'spec_helper'

RSpec.describe "MailPredictor::App::PredictionsHelper" do
  describe "add some examples to (or delete) #{__FILE__}" do
    let(:helpers){ Class.new }
    before { helpers.extend MailPredictor::App::PredictionsHelper }
    subject { helpers }
  end
end
